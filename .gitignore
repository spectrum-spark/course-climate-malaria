.Rproj.user
.Rhistory
.RData
.Ruserdata

/.quarto/

# Quarto/knitr intermediate left behind by an interrupted or failed render
*.rmarkdown

# Rendered site (rebuilt locally; _freeze is committed so it builds from cache)
/_site/

# Large shared assets — the climate data AND the Session 1 slides live in /data/,
# shared separately (not tracked in git). See Session 2, Part 1 for the original
# sources (Copernicus CDS, Open Development Mekong, DDC malaria API). Keep a local
# copy of /data/ to re-run the analysis; the committed _freeze/ lets the site
# render without it.
/data/

# Python workflow artefacts — not committed for now. The updated notebook will be
# added by the team once available (the optional Python reference for keen
# students): drop the `.ipynb` line below (or `git add -f`).
python_workflow/malaria_workflow.html
python_workflow/docs/
python_workflow/malaria_workflow.ipynb

# Generated outputs — figures, CSVs and JSON. The R course writes to the top-level
# outputs/; the Python reference writes to python_workflow/outputs/. compare.R
# diffs the two panel JSONs.
/outputs/
python_workflow/outputs/

# Personal scratch/test scripts (not part of the course materials)
local_test.R

# Regenerable copy-paste output of make_area_weight_diagram.py (the SVG itself lives
# inline in the .qmd; this file is just a scratch source to copy from)
session2_climate_data/area_weight_diagram.svg

# IDE config
.idea/

# macOS
.DS_Store
